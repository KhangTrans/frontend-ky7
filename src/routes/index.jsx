// Export routes đã được generate từ JSON config
export { default } from './generateRoutes.jsx';

// Export các config khác nếu cần
export { default as routesData } from './routes.config';
export { default as componentsMap } from './components';
